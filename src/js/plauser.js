/**
 * Super class for the main plauser classes.
 */
function Plauser() {}

/**
 * Simply injects some operations that play/pause the current
 * track.
 * @param tabId the id of the tab used to identity.
 */
Plauser.prototype.playPause = function (tabId) {};